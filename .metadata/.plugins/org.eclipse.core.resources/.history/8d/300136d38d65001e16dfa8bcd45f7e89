package application.controller;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.control.Hyperlink;

public class MainController {

    @FXML
    private Hyperlink hyperlink;

    @FXML
    private void handleLinkClick(ActionEvent event) {
        // Handle the hyperlink click event here
        String link = "Page2.fxml"; // Set your relative link here

        // Load the new FXML content based on the relative link
        FXMLLoader loader = new FXMLLoader(getClass().getResource(link));
        try {
            Parent newContent = loader.load();
            // Replace the current content with the new content
            // For example, assuming you have a StackPane as your root:
            StackPane root = (StackPane) hyperlink.getScene().getRoot();
            root.getChildren().clear();
            root.getChildren().add(newContent);
        } catch (IOException e) {
            e.printStackTrace();
            // Handle any potential exceptions related to loading the new content
        }
    }
}